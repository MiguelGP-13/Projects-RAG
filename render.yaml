services:
  - type: web
    name: redis-stack
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: REDIS_PASSWORD
    plan: starter
    autoDeploy: true
    ports:
      - port: 6379
